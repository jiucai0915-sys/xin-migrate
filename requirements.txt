# 端侧模型走 Ollama 的 OpenAI 兼容接口
openai>=1.0.0

# 环境变量
python-dotenv>=1.0.0

# 验证用数据库（postgres 模拟；生产换达梦/金仓驱动）
psycopg2-binary>=2.9.0
sqlparse>=0.4.0

# 前端（演示界面，web/app.py 用 Streamlit）
streamlit>=1.30.0

# 测试（可选）：pytest -q
pytest>=7.0.0
